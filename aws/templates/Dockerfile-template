# 🐳 Dockerfile 템플릿
# 팀원들이 각자의 서비스에 맞게 수정하여 사용하세요.

# Java 21 기반 이미지 사용
FROM openjdk:21-jdk-slim

# 작업 디렉토리 설정
WORKDIR /app

# 애플리케이션 JAR 파일 복사
# build/libs/ 디렉토리에서 JAR 파일을 찾아 복사
COPY build/libs/*.jar app.jar

# 서비스별 포트 노출
# User Service: 8082, Store Service: 8081, Reservation Service: 8080
EXPOSE 8080

# 애플리케이션 실행
CMD ["java", "-jar", "app.jar"]

# =============================================================================
# 사용법:
# 1. 이 파일을 각 서비스 루트 디렉토리에 Dockerfile로 저장
# 2. EXPOSE 포트를 서비스에 맞게 수정:
#    - User Service: EXPOSE 8082
#    - Store Service: EXPOSE 8081  
#    - Reservation Service: EXPOSE 8080
# 3. JAR 파일 경로가 다르면 COPY 명령어 수정
# =============================================================================
